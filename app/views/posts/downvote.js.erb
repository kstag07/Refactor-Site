$('post-<%=@post.id%>').html('<%= @post.votes_for.size %>');

$('.votedown').click(function() {
  $(this).toggleClass('vote-toggle')
  if ($('.voteup').hasClass('.vote-toggle') {
    $('.voteup').toggleClass('vote-toggle')
  });
});